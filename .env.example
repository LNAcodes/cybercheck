# MongoDB connection string (MongoDB Atlas recommended)
MONGODB_URI=

# NextAuth.js secret — generate with: openssl rand -base64 32
NEXTAUTH_SECRET=

# Public URL of your app (used by NextAuth for callbacks)
NEXTAUTH_URL=http://localhost:3000
